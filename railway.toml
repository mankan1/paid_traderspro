[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python build.py && uvicorn server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/me"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
